# 2026-09-06 当日日志

## 1. 主题
Phase 0：为类 Notion 平台（Java + Gradle + bun）搭建 `.agents` 与 `AGENTS.md` / `CLAUDE.md`。

## 2. 完成项

| 产物 | 说明 |
|---|---|
| `AGENTS.md` | 工程约定：技术栈（Java 21 / Gradle / Spring Boot 3 / bun workspaces / lefthook / commitlint）、架构铁律、scope 白名单、任务清单 T1–T12、环境与坑 |
| `CLAUDE.md` | 入口文件，默认 `@AGENTS.md` 引用，不重复维护 |
| `.agents/rules/vision.md` | 产品愿景与边界（AI 双向转换 + 可追溯为主线） |
| `.agents/workflow/handoff.md` | 会话交接模板（5 分钟内进入状态） |
| `.agents/workflow/milestone-flow.md` | 方案优先 SOP + 验证矩阵（Gradle/bun 适配） |
| `.agents/workflow/review-checklist.md` | 多 Agent 互审清单（Java/Spring/模块化单体适配） |
| `.agents/workflow/scratchpad-concurrency.md` | 多 Agent 并发约定（scratchpad + worktree） |
| `.agents/tools/check.sh` | 仓库一致性检查（零依赖，兼容未 git init） |
| `.agents/history/MEMORY.md` | 长期记忆（ADR 索引 + 里程碑 + 待办） |
| `docs/` | 同步《MVP开发交接文档》《AI-Agent开发规范》 |

（均未 commit——项目尚未 git init。）

## 3. 踩坑
- 无（配置阶段）。环境注意已写入 AGENTS.md §7：JDK 8 在 PATH 但不可用，需装 Java 21；bun 不在默认 PATH。

## 4. 验证
- `bash .agents/tools/check.sh` → 预期 5 项检查全绿（docs 存在、scope 比对跳过、workspaces 跳过、无 .git 跳过、AGENTS.md 章节齐全）。
- 待用户确认后：git init → 装依赖 → lefthook install → commitlint 实测。

## 5. 待办
- [ ] 用户确认项目名（工作名 `notion-like`）
- [ ] T1 脚手架：bun workspaces + Gradle wrapper + docker-compose
- [ ] 设 JAVA_HOME / sdkman 默认指向 GraalVM 25（技术栈已定版：Java 25 + Spring Boot 4.1.1，2026-09-06 追加）
- [ ] commitlint + lefthook 落地并实测
- [ ] git init + 首次提交

## 6. 工作树状态
- `~/code/notion-like` 尚未创建（配置暂存于 Windows 侧待同步）；main 无改动；无 worktree。

> 2026-09-06 追加：技术栈定版 **Java 25（LTS）+ Spring Boot 4.1.1**（Boot 4 基于 Spring Framework 7 / Jakarta EE 11，Web starter 用 `spring-boot-starter-webmvc`）。上文中「Java 21 / Spring Boot 3」为初稿状态，一切以 AGENTS.md 为准。
> 2026-09-06 追加：**移动端 Flutter 在 Windows 侧开发**（WSL 不适合 Flutter 设备调试/工具链）；同一仓库用 git worktree 在 Windows 侧检出 `mobile` 分支开发，提交归一。
> 2026-09-06 追加：项目正式定名 **TransNote**（仓库 `~/code/transnote`，Java 包 `com.transnote.*`），上文「Notion-like / notion-like」均为工作名，以 TransNote 为准。
>
> **构建落地（2026-09-06 当日）**：
> - `git init -b main`，首提交 `chore(repo): 初始化 TransNote 工程约定与 git hooks`（f79ddf4，28 files）
> - **commitlint + lefthook**：@commitlint/cli 21.2.2 / lefthook 2.1.12（bun 安装）；坏提交 `feat(badscope)` 被 commit-msg 拦截实测 ✅；pre-commit 密钥扫描 `check-secrets.sh`；lefthook.yml 钩子命令显式 export PATH（bun 不在默认 PATH）
> - **semantic-release**：25.0.9 + changelog 7.0.0 + git 11.0.1 + conventionalcommits 10.4.0（lanchat 现役组合）；起点 tag `v0.0.0`；dry-run 验证 `fix(repo)` → next release **0.0.1** ✅；release.config.js 本地阶段不接 npm/GitHub 插件
> - 无 GitHub remote → 建本地 bare `~/code/transnote.git` 作 origin 占位（接 GitHub 时 `git remote set-url origin` 即可）
> - 修复：check.sh scope 比对（GNU sed 范围多打一行 + 代码围栏空行）、check-secrets.sh 正则加 `-e` 防私钥模式被当选项
> - **发布 v0.0.1**（2026-09-06）：`bun run release` 全自动完成——计算版本 0.0.1 → 生成 CHANGELOG.md → `chore(release): 0.0.1 [skip ci]` 自动提交 → `Created tag v0.0.1`；发布链路实测通过
> - 发布踩坑链（已记入 AGENTS.md §6/§7）：①本地路径 remote 导致 `new URL` 报错 → repositoryUrl 显式改 `file://` 占位；②conventionalcommits 是插件的 peerDependency，bun 不自动装，显式装 `9.3.1`（10.x 会 Missing helper）；③bun 的 prepare 不注入 .bin → `bunx lefthook install`；④`bun remove` 误删 commitlint 依赖需恢复；⑤CHANGELOG 中途失败会残留重复块需清理
> **T1 后端第一步（2026-09-06）**：
> - Spring Boot **4.1.1** 基础工程：`server/`（Gradle 9.6.1 wrapper + Kotlin DSL + Version Catalog；Java 25 toolchain；modules/shared + modules/webmvc；`TransnoteApplication` + actuator + contextLoads 测试）——`./gradlew build` 通过（1m29s）
> - spotless **8.10.1** 接入：google-java-format（2 空格）+ removeUnusedImports + trimTrailingWhitespace + endWithNewline；`spotlessApply`/`spotlessCheck` 通过
> - 新坑（已记入 AGENTS.md §7）：①services.gradle.org 307 导致 wrapper 验证失败 → wrapper distributionUrl 用腾讯镜像；②Gradle Kotlin DSL `subprojects` 内 `java {}` 语法糖编译期不可用 → `extensions.configure<JavaPluginExtension>`；③spotless 8.x 的包名是 `com.diffplug.gradle.spotless.SpotlessExtension`（不是 com.diffplug.spotless.gradle）
> **基础设施版本升级（2026-09-06）**：
> - PostgreSQL **18**（18.6 为最新稳定小版本；PG 19 截至 2026-09-06 仍为 Beta 3 未 GA，不采用）——替代原 16
> - **Valkey 9.1.2**（2026-09-01 发布，Linux 基金会，Redis 协议兼容）——替代原 Redis 7；Java 侧连接串 scheme 仍为 redis://
> - MinIO：`minio/minio:latest`（跟随最新 RELEASE）；ES 示例同步升 **9.5.3**（后置依赖）
> - 落地：`infra/docker-compose.yml`（postgres:18 + valkey/valkey:9.1.2 + minio，含 healthcheck）；数据库名统一为 `transnote`（清掉 notion_like 残留）；AGENTS.md 与《MVP 开发交接文档》同步
> **Spring Data JPA 集成（2026-09-06）**：starter-data-jpa（Boot 4 内含 Hibernate 7）+ postgresql 42.7.13；datasource `jdbc:postgresql://localhost:5433/transnote`（dev/dev），ddl-auto none（迁移方案 T2 前定），open-in-view false。contextLoads 真实连库通过（commit dbefa9c）。
> **依赖环境落地**：Docker Desktop（Windows 侧）+ daocloud 镜像加速；PG18/valkey9.1.2/minio 全部 healthy；端口 5433/6379/9000（5432 被 ai-copilot 占用）。坑见 AGENTS.md §8。
> **Flyway 接入 + V1 workspace 表（2026-09-06）**：starter-flyway + flyway-core/database-postgresql **12.11.0**（BOM 12.4.0 不支持 PG18，踩坑修复）；`db/migration/V1__init_workspace.sql` 建 workspaces 表；build+迁移落库验证通过（flyway_schema_history version=1 success）；commit fe87a19（曾因脚本管道吞失败码先提交了坏 commit，已 amend 为干净提交）。
> **T2 工作区 CRUD 完成（2026-09-06，commit fc81ba7）**：新增 modules/identity（Workspace 实体/Repository/Service + 异常），webmvc 装配 API（ApiResponse 统一包裹 + GlobalExceptionHandler + WorkspaceController）；8 个单测 + 4 个集成测试全过；真实服务冒烟（POST/GET/PATCH/DELETE/422/404）全链路通过。中文名 slug 派生为空时回退 ws- 前缀。Boot 4 坑：MockMvc 拆到 spring-boot-starter-webmvc-test（包名 org.springframework.boot.webmvc.test.autoconfigure）；Gradle 9 需显式 junit-platform-launcher。
> **T3 文档/块 CRUD 完成（2026-09-07，commit 5068b80 + 5efe9e3）**：V2 迁移 documents/blocks；modules/document（Document/Block 实体、仓库、DocumentService/BlockService：upsert/delete/move 防环/整树组装）+ REST（POST/GET /documents、GET/PATCH/DELETE /{id}、PATCH /{id}/blocks）。11 单测 + 3 集成测试 + 全链路冒烟通过。坑：Boot 4 Jackson 3（tools.jackson）、PG jsonb 需 @JdbcTypeCode(SqlTypes.JSON)、findByWorkspace_Id、@Version 初始值。GET versions 端点待快照表（T6 后置）。
> **T3 文档/块 CRUD 完成（2026-09-07，commit 5068b80 + 5efe9e3）**：V2 迁移 documents/blocks；modules/document（Document/Block 实体、仓库、DocumentService/BlockService：upsert/delete/move 防环/整树组装）+ REST（POST/GET /documents、GET/PATCH/DELETE /{id}、PATCH /{id}/blocks）。11 单测 + 3 集成测试 + 全链路冒烟通过。坑：Boot 4 Jackson 3（tools.jackson）、PG jsonb 需 @JdbcTypeCode(SqlTypes.JSON)、findByWorkspace_Id、@Version 初始值。GET versions 端点待快照表（T6 后置）。
> **T5 看板 CRUD 完成（2026-09-07，commit 70691e9 + 154b810 + 6a7d66b）**：V3 迁移 boards/board_columns/board_cards；modules/board（Board/BoardColumn/BoardCard、仓库、BoardService：建板/列/卡、拖拽换列重排一次提交、部分更新、筛选、级联删除）+ REST。10 单测 + 3 集成测试 + 全链路冒烟通过。坑：Boot 4 MockMvc 新包名 org.springframework.boot.webmvc.test.autoconfigure；跨列 move 需"旧列移除不插回"独立方法；删列后按该列筛选返回空列表。
> **T6 Word 解析 + 分块完成（2026-09-07，commit cbc5086 + d39e439）**：modules/conversion（POI 5.5.1 + DocElement record：HEADING/PARAGRAPH/TABLE/CHECKBOX/LIST + paragraphIndex/rawRange 锚点 + Chunker 按 heading 切块 ≤2000 token 表格不拆）+ 解析调试端点。5 单测 + 2 集成测试 + 真实 docx 冒烟通过（标题层级/☐☑ 勾选/表格整体/分块/旧格式 422）。POI 5.5.1 三个 API 坑已记 AGENTS.md。
> **T7 LLM Provider + 抽取完成（2026-09-07，commit d781050 + c0923fa）**：LlmProvider 抽象（OpenAI 兼容 /chat/completions + JDK HttpClient + JSON Schema response_format）+ TaskExtractor 门面（LLM 优先、规则回退 rule-v1、evidence 强制校验、prompt_version 记录）+ 固定 prompt 模板（prompts/task-extraction.txt，含 2 few-shot）+ RuleTaskExtractor 基线（checkbox/list/table/heading→category/段落→description）。5 单测 + 2 集成测试 + 真实 docx 冒烟通过（勾选+表格任务、evidence 锚点、负责人/截止解析）。
> **T8 Word→看板 全链路完成（2026-09-07，commit 25539df + 5bdb01c）**：V4 迁移（conversion_jobs/items + board_cards.assignee_name）+ ConversionService 编排（落盘→解析→抽取→items→置信度分流→自动建板/REVIEW→校对后建板）+ 4 端点 + LocalAssetStorage（MinIO 后置）。5 单测 + 4 集成测试 + 真实 docx 冒烟通过：3 任务自动建板、evidence 追溯、表格负责人/截止解析、assignee_name 落卡。
> **T9 看板→Word 导出完成（2026-09-07，commit 76fbcd1 + 545a7be）**：V5 迁移（source_board_id/result_asset_id/template）+ WordExporter（聚合统计/完成率/延期项 + 表头底纹 + ☐☑ + 页码域 + 回读校验）+ submitBoardToWord 同步编排 + board-to-word 端点 + 产物下载端点。5 单测 + 1 集成测试 + 真实服务冒烟通过：看板 2 卡（待办/已完成）导出 docx 3207B，标题/统计 50%/截止日期/☐☑ 全部核验命中。
> **GitHub 接入 + CI 全绿（2026-09-07，commit 874bb2b + af3f1a0 + 9354724）**：gh repo create pandaymx/transnote（私有）→ workflow（postgres:18 service + temurin25 + spotlessCheck + clean build / bun commitlint / semantic-release）→ v1.0.0 自动发布（CHANGELOG + tag + Release，commit-analyzer 识别全部 feat）。首跑踩坑：@semantic-release/github success 评论需 issues/pull-requests write 权限（补上后全绿）；checkout/setup-java 升 v5 消 deprecation。
> **T10 Golden 回归集（2026-09-07，2659c81）**：22 样例（勾选/编号/表格/标题/空边界）+ expected.json + 阈值断言，title 44/44=100%、assignee 7/7=100%、due 7/7=100%。踩坑：@TempDir 随机目录改 build/golden-gen、cp 误入 init 文件。
> **T4a 前端骨架（2026-09-07，81ea3ba + 1b1c832 + a384071）**：packages/schema（JSON Schema + TS 类型）+ packages/api-client（类型化 fetch 封装）+ packages/core（TanStack Query hooks + Zustand）+ apps/web（Next 16 App Router：工作区/看板列表/看板详情/转换页含 word→board 上传+校对+board→word 导出）。CI 新增 web job 全绿；冒烟：3 路由 200 + CORS 头正确。踩坑：.next 误提交（gitignore 补救）、request 漏传 baseUrl、PowerShell $ 转义。
> **线性合并约定（2026-09-07，6e41ce1）**：用户要求线性历史。本地配置 pull.rebase=true / merge.ff=only / branch.main.rebase=true；历史用 rebase --onto 重构（a384071→bc95318→dcb470d→8102796，去 merge 提交）；GitHub 分支保护 required_linear_history=true；AGENTS.md 记录"禁止 merge 提交、rebase+force-with-lease"约定。
> **T4b Tauri 桌面壳（2026-09-07，597cf20 + 1a60f4b + 9432c4c）**：apps/desktop（vite+react，端口 3001）+ src-tauri（Cargo.toml/buid.rs/main.rs/lib.rs/tauri.conf.json/capabilities），复用 packages/core 做工作区列表页。CI 新增 desktop job（tsc + cargo check，apt 装 webkit2gtk）。踩坑：缺 icon.png 编译失败、[lib] name 未声明 E0433。v1.2.1 已发。全部 job 绿。
> **T4c 看板交互（2026-09-07，9bcc378）**：api-client 补 updateCard(patch)/deleteCard；core 补 useUpdateCard/useDeleteCard（乐观更新+失败回滚）；看板详情页实现 HTML5 拖拽换列（position=目标列数量）、双击内联编辑、删除、添加卡片。冒烟全链路通过（建板→拖拽换列→编辑→删除→数量断言）。踩坑：PowerShell 内联 `\"` 注入导致 Jackson escape 报错（必须 .sh 文件单引号 JSON）；pkill 正则误杀 gradle 子进程。
> **T5 转换历史（2026-09-07，1f3e6bc + 432394f）**：GET /api/v1/conversions/jobs?workspaceId=（近50条倒序轻量）+ ConversionService.listJobs + 测试 jobsList_returnsHistoryNewestFirst；前端 useJobs + convert 页历史区块（方向/状态/时间/看板/下载入口）；契约 §7.2/§9.2 同步。
> **T6 导出入口+桌面三视图（2026-09-07，47061a9 + c23191d）**：看板页"导出 Word"按钮（?tab=export 自动带入 convert 页导出区）；桌面壳 工作区→看板→看板详情 三视图（复用 core，state 切换路由）。v1.4.0 已发。教训：desktop 端 TanStack 推断丢交集类型（board.columns 需显式断言）；setView 需要 workspaceId 字段。
> **LLM 启用说明**：§8.3 写入环境变量启用方式（LLM_BASE_URL/API_KEY/MODEL），无 key 时规则抽取兜底。
> **T8 回归修复 + 浏览器 e2e 全链路（2026-09-07，cc97bac + 6548a56）**：首次真实浏览器 e2e（bu + Next dev + bootRun）抓出两个 CI 盲区 bug：① api-client request() 带 body 不设 Content-Type → 浏览器默认 text/plain 致后端 415（curl 测不到，仅浏览器触发）；② Word 转换带描述任务（g04 类）description 纯文本直传 JSONB 列校验失败 → rollback-only 吞根因致 500（临时 @SpringBootTest 非代理直调定位），buildBoard 新增 toDescriptionJson 包装，补集成测试。全链路验证通过：建工作区→建板→上传转换 COMPLETED（描述 JSONB 落库 {
"text":...}）→ 开板验证卡片 → 导出入口 tab=export 自动带入 → 导出 docx 下载回读（统计头+任务表）。v1.5.0/v1.5.1 已发。
> **桌面端 deb 打包（2026-09-07，4d6bb76）**：CI 新增 desktop-bundle job（needs desktop）——git tag 作产物版本（sed 去 v），bunx tauri build --bundles deb + upload-artifact 上传。验证：TransNote_1.5.2_amd64.deb（1.45MB，usr/bin/transnote-desktop + TransNote.desktop + hicolor 128px 图标），6 job 全绿。upload-artifact Node20 deprecation 为无害告警。
